# /check/ and /legal/ are intentionally NOT Disallowed here even though
# both carry <meta name="robots" content="noindex, nofollow">: blocking a
# noindex page via robots.txt stops crawlers from ever reading that meta
# tag, which is the documented anti-pattern (Google can still list a
# robots.txt-blocked URL with no snippet). The per-page noindex meta tag
# is the correct, sufficient mechanism for both routes.
User-agent: *
Allow: /

Sitemap: https://taviv.co.il/sitemap.xml
